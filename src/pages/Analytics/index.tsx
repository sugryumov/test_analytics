import { FC } from 'react';
import { Header } from './Header';
import { Cards } from './Cards';

export const Analytics: FC = () => {
  return (
    <div>
      <Header />
      <Cards />
    </div>
  );
};
